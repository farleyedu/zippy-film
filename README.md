# Zippy Film

Frontend TV-first para Jellyfin.

## Deploy Vercel

O projeto Vite fica em `film front`, mas a raiz tem `package.json`, `vite.config.ts` e `vercel.json` para o Vercel buildar corretamente.
